Note to myself: 
- No tags - they tend to make the folder subcategories clunky when trying to navigate
- Turn off the date features
- Try to do one page per day. 

**Syncing Changes**
or 
```npx quartz sync```
or 
```
git add .
git commit -m "change site header to ISCAMP"
git push origin v4  # Or main if using main
```

